# 指令微调数据构造指南 | 让模型听懂人话

指令微调（Instruction Tuning）是让模型从"文字接龙"变成"听指令做事"的关键步骤。本文系统讲解数据集构造方法论。

## 一、指令微调的数据结构

### Alpaca 格式
```json
{
  "instruction": "将以下句子翻译成英文",
  "input": "今天的天气真不错",
  "output": "The weather is really nice today."
}
```

### ShareGPT 格式（多轮对话）
```json
{
  "conversations": [
    {"from": "human", "value": "你好，我想学 Python"},
    {"from": "gpt", "value": "太好了！你想从哪方面开始学？"},
    {"from": "human", "value": "我想做数据分析"},
    {"from": "gpt", "value": "那我建议你从 Pandas 开始，这是 Python 最强大的数据分析库..."}
  ]
}
```

### 系统指令格式
```json
{
  "conversations": [
    {"from": "system", "value": "你是一个专业的 Python 技术导师，用通俗易懂的语言解答问题。"},
    {"from": "human", "value": "列表和元组有什么区别？"},
    {"from": "gpt", "value": "列表（List）和元组（Tuple）的最大区别是：列表可变，元组不可变..."}
  ]
}
```

## 二、数据多样性设计

### 任务类型分布
```python
TASK_DISTRIBUTION = {
    "开放式生成": "30%",    # 创意写作、文案、邮件
    "知识问答": "25%",      # 事实问答、解释概念
    "分析推理": "15%",      # 数据分析、逻辑推理
    "代码编程": "10%",      # 写代码、Debug、解释代码
    "分类判断": "10%",      # 情感分析、意图判断
    "总结提取": "10%",      # 摘要、关键信息提取
}
```

### 指令多样性 Checklist
```python
# 指令应当覆盖：
"""
✅ 不同长度的指令（3个词 → 200个词）
✅ 不同格式要求（纯文本、JSON、Markdown、表格）
✅ 不同语气（正式、口语化、幽默）
✅ 不同角色设定（专家、助手、教师、朋友）
✅ 中文为主 + 少量英文 + 中英混合
✅ 单轮 + 多轮对话
✅ 有正确答案 + 无标准答案（开放性）
"""
```

## 三、数据构造方法

### 方法 1：Self-Instruct
```python
# 从少量种子任务自动生成更多任务
SEED_INSTRUCTIONS = [
    {"instruction": "将中文翻译成英文", "input": "你好", "output": "Hello"},
    {"instruction": "计算以下数学题", "input": "2+3×4", "output": "14"},
    # 8-10 条种子即可
]

def self_instruct(seed_instructions, model, num_generate=100):
    """自我生成指令数据"""
    generated = []
    
    while len(generated) < num_generate:
        # 随机选几条种子作为 prompt 示例
        few_shot = random.sample(seed_instructions + generated, min(8, len(seed_instructions) + len(generated)))
        
        # 让模型生成新指令
        prompt = f"""基于以下示例，生成一条新的指令任务。要求：
1. 与示例不重复
2. 覆盖不同的领域和类型
3. 输出 JSON 格式：{{"instruction":"...", "input":"...", "output":"..."}}

示例：{json.dumps(few_shot, ensure_ascii=False, indent=2)}
"""
        new_task = model.generate(prompt)
        generated.append(parse_json(new_task))
    
    return generated
```

### 方法 2：Evol-Instruct
```python
# 逐步增加指令的复杂度
def evolve_instruction(task: dict, model, depth=3) -> List[dict]:
    """将一个简单指令逐步复杂化"""
    evolved = [task]
    
    for _ in range(depth):
        prompt = f"""请将以下指令变得更深、更复杂。可以：
- 增加约束条件（如限制输出格式、字数）
- 增加推理步骤
- 增加多步操作的串联
- 增加边界情况处理

原指令：{json.dumps(evolved[-1], ensure_ascii=False)}

输出新的 JSON（只输出 JSON，不解释）："""
        
        harder = model.generate(prompt)
        evolved.append(parse_json(harder))
    
    return evolved
```

### 方法 3：Backtranslation
```python
def backtranslate(task: dict, model) -> dict:
    """反向翻译增加多样性"""
    # 1. 将中文指令翻译成英文
    en_prompt = f"Translate to English:\n{task['instruction']}\n{task.get('input', '')}"
    en_instruction = model.generate(en_prompt)
    
    # 2. 翻译回中文
    zh_prompt = f"翻译成中文：\n{en_instruction}"
    zh_instruction = model.generate(zh_prompt)
    
    return {
        "instruction": zh_instruction,
        "input": task.get("input", ""),
        "output": task["output"]
    }
```

## 四、数据质量控制

### 自动过滤
```python
def quality_filter(dataset: List[dict]) -> List[dict]:
    """自动过滤低质量数据"""
    filtered = []
    
    for item in dataset:
        # 1. 长度检查
        instruction = item.get("instruction", "")
        output = item.get("output", "")
        
        if len(instruction) < 5 or len(output) < 20:
            continue
        
        # 2. 重复度检查
        if instruction in output:
            continue  # 指令和输出高度重复
        
        # 3. 截断检查
        if output.endswith("...") or output.endswith("等等"):
            continue  # 可能被截断
        
        # 4. 无效内容
        skip_phrases = ["我是一个AI", "作为AI语言模型", "我不是真的"]
        if any(phrase in output for phrase in skip_phrases):
            continue  # 包含拒绝回答
        
        # 5. 语言一致性
        if detect_instruction_lang(instruction) != detect_response_lang(output):
            # 指令是中文但回答是英文（除非明确要求翻译）
            continue
        
        filtered.append(item)
    
    return filtered
```

### 人工审核抽样
```python
# 随机抽 10% 进行人工检查
sample_size = max(10, len(dataset) // 10)
samples = random.sample(dataset, sample_size)

# 评分维度
RUBRIC = {
    "答案准确性": "1-5分",
    "格式规范性": "1-5分", 
    "语言流畅度": "1-5分",
    "是否有幻觉": "是/否",
}

# 如果准确率 < 90%，需要改进数据质量
```

### 去重
```python
from difflib import SequenceMatcher

def deduplicate_instructions(dataset: List[dict], threshold=0.85) -> List[dict]:
    """基于指令相似度去重"""
    unique = []
    for item in dataset:
        is_dup = False
        for existing in unique:
            sim = SequenceMatcher(None, item["instruction"], existing["instruction"]).ratio()
            if sim > threshold:
                is_dup = True
                break
        if not is_dup:
            unique.append(item)
    return unique
```

## 五、数据构造实践

### 高质量数据特征
```
✅ 输出长度适中：100-500 tokens（不要太短或太长）
✅ 逻辑完整：能独立理解，不需额外上下文
✅ 事实正确：无幻觉和编造
✅ 格式清晰：需要结构化时就给出结构
✅ 风格统一：同一角色的回答风格一致
✅ 安全合规：不涉及有害、违法内容
```

### 数据规模建议
```
入门验证：100-500 条（跑通流程）
正式微调：1,000-5,000 条（基本可用）
生产级别：5,000-20,000 条（效果稳定）
大规模微调：50,000+ 条（追求极致）
```

## 总结

> 指令微调的本质：用高质量的数据，教会模型"什么是好的回答"。

数据构造三原则：**多样覆盖、质量优先、格式统一。**
