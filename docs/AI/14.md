# LLaMA-Factory 一站式微调上手 | 从零到部署

LLaMA-Factory 是目前最好用的大模型微调框架，支持 SFT / LoRA / QLoRA / DPO / RLHF，且带 Web UI。

## 一、快速开始

### 安装
```bash
git clone https://github.com/hiyouga/LLaMA-Factory.git
cd LLaMA-Factory
pip install -e ".[torch,metrics]"

# 验证安装
llamafactory-cli version
```

### Web UI 启动
```bash
# 启动 Web 界面（推荐新手使用）
llamafactory-cli webui

# 访问 http://localhost:7860
```

## 二、Web UI 操作流程

### Step 1：选择模型
```
模型选择 → Qwen/Qwen2.5-7B-Instruct
微调方法 → LoRA
```

### Step 2：配置数据集
```
数据集 → 选择内置或上传自定义数据

自定义数据上传：
1. 将 JSON 文件放入 LLaMA-Factory/data/
2. 在 dataset_info.json 中注册
```

### Step 3：配置参数
```
LoRA rank: 8
Epochs: 3
Learning rate: 2e-5
Batch size: 4
```

### Step 4：开始训练
```
点击 "开始" → 观察 loss 曲线 → 训练完成
```

## 三、命令行使用

### 注册自定义数据集
```json
// data/dataset_info.json 中添加
{
  "my_dataset": {
    "file_name": "my_data.json",
    "columns": {
      "prompt": "instruction",
      "query": "input",
      "response": "output"
    }
  }
}
```

### 训练命令
```bash
# SFT LoRA 微调
llamafactory-cli train \
  --model_name_or_path Qwen/Qwen2.5-7B-Instruct \
  --stage sft \
  --do_train \
  --finetuning_type lora \
  --lora_rank 8 \
  --lora_alpha 16 \
  --lora_target all \
  --dataset my_dataset \
  --template qwen \
  --output_dir ./output \
  --per_device_train_batch_size 4 \
  --gradient_accumulation_steps 4 \
  --lr_scheduler_type cosine \
  --logging_steps 10 \
  --save_steps 500 \
  --learning_rate 2e-5 \
  --num_train_epochs 3 \
  --bf16
```

### 对话测试
```bash
# 在训练的模型上对话
llamafactory-cli chat \
  --model_name_or_path Qwen/Qwen2.5-7B-Instruct \
  --adapter_name_or_path ./output \
  --template qwen
```

### 模型导出
```bash
# 合并 LoRA 权重并导出
llamafactory-cli export \
  --model_name_or_path Qwen/Qwen2.5-7B-Instruct \
  --adapter_name_or_path ./output \
  --template qwen \
  --finetuning_type lora \
  --export_dir ./merged_model \
  --export_size 2 \
  --export_legacy_format false
```

## 四、高级功能

### 多数据集混合
```yaml
# data/dataset_info.json
{
  "my_sft_data": {"file_name": "sft_data.json", "columns": {...}},
  "my_dpo_data": {"file_name": "dpo_data.json", "columns": {...}}
}

# 训练时混用
--dataset my_sft_data,my_sharegpt_data,alpaca_zh
# 可混合多个数据集一起训练
```

### 数据比例混合
```bash
# 不同数据集按比例混用
--dataset my_domain_data,general_data
--dataset_dir data/
# 在 dataset_info.json 中配置比例
```

### 多轮对话训练
```json
// data/my_chat_data.json
[
  {
    "conversations": [
      {"from": "human", "value": "你好，我想订机票"},
      {"from": "gpt", "value": "好的，请问您想从哪个城市出发？"},
      {"from": "human", "value": "从北京到上海"},
      {"from": "gpt", "value": "请问您想什么日期出发？"},
      {"from": "human", "value": "下周二"},
      {"from": "gpt", "value": "好的，为您查询到了以下航班信息..."}
    ]
  }
]
```

### DPO 训练
```bash
llamafactory-cli train \
  --model_name_or_path ./sft_output \
  --stage dpo \
  --do_train \
  --finetuning_type lora \
  --lora_rank 8 \
  --dataset my_dpo_data \
  --template qwen \
  --pref_beta 0.1 \
  --output_dir ./dpo_output \
  --per_device_train_batch_size 2 \
  --gradient_accumulation_steps 8 \
  --learning_rate 5e-7 \
  --num_train_epochs 1 \
  --bf16
```

## 五、多卡训练

```bash
# 使用 DeepSpeed ZeRO-2（省显存）
deepspeed --num_gpus=4 src/train.py \
  --deepspeed examples/deepspeed/ds_z2_config.json \
  --model_name_or_path Qwen/Qwen2.5-14B-Instruct \
  ...其他参数

# 使用 FSDP（CPU offload）
# 在参数中加：
--fsdp "full_shard auto_wrap offload"
```

### DeepSpeed 配置示例
```json
{
  "train_batch_size": "auto",
  "train_micro_batch_size_per_gpu": "auto",
  "gradient_accumulation_steps": "auto",
  "zero_optimization": {
    "stage": 2,
    "offload_optimizer": {
      "device": "cpu",
      "pin_memory": true
    },
    "allgather_partitions": true,
    "allgather_bucket_size": 2e8,
    "overlap_comm": true,
    "reduce_scatter": true,
    "reduce_bucket_size": 2e8,
    "contiguous_gradients": true
  }
}
```

## 六、常见问题

### Q1：训练中断了怎么恢复？
```bash
# LLaMA-Factory 会自动保存 checkpoint
# 指定 checkpoint 路径继续训练
--resume_from_checkpoint ./output/checkpoint-500
```

### Q2：如何只用 CPU 训练？
```bash
# CPU 训练很慢，仅用于测试
--device cpu
# 建议至少用一块 GPU
```

### Q3：训练多久？
```
7B LoRA, 4090, 3000条数据, batch=16：
- 3 epochs → ~1.5 小时

7B QLoRA, 4060, 3000条数据, batch=16：
- 3 epochs → ~3 小时

70B LoRA, A100, 3000条数据, batch=4：
- 3 epochs → ~6 小时
```

### Q4：显存不够怎么办？
```
优先级从高到低：
1. 用 QLoRA（4bit 量化）
2. 减小 batch_size + 增加梯度累积
3. 减小模型 max_length
4. 开启 gradient_checkpointing
5. 减小 LoRA rank
```

## 七、最佳实践流程

```
1. 准备数据：JSON 格式，放入 data/ 目录
2. 注册数据：编辑 data/dataset_info.json
3. Web UI 试跑：确认数据加载正确
4. 小规模试训：1 epoch, 100 条数据，确认 loss 下降
5. 正式训练：全量数据，多 epoch
6. 对话测试：llamafactory-cli chat 测试效果
7. 导出模型：merge_and_export 部署使用
```

## 总结

> LLaMA-Factory = 大模型微调的最短路径。

Web UI 入门，命令行提效，DeepSpeed 多卡，DPO 对齐——一个框架全覆盖。
