# 数据采集与清洗实战 | AI 项目的数据根基

数据质量决定模型上限。本文详解 AI 项目中的数据采集策略与清洗方法论。

## 一、数据来源全景

### 1. 公开数据集
| 平台 | 特点 | 地址 |
|------|------|------|
| Hugging Face Datasets | 最大 NLP 数据集库，直接 API 加载 | huggingface.co/datasets |
| Kaggle | 竞赛数据集，覆盖各领域 | kaggle.com |
| OpenDataLab | 国内平台，下载速度快 | opendatalab.com |
| Modelscope（魔搭） | 阿里出品，中文数据集丰富 | modelscope.cn |
| GitHub Archive | 代码语料 | gharchive.org |

### 2. 自有数据采集
```python
# 网页爬取示例
import requests
from bs4 import BeautifulSoup

def crawl_articles(url, max_pages=100):
    """爬取网站文章内容"""
    articles = []
    for page in range(1, max_pages + 1):
        resp = requests.get(f"{url}?page={page}")
        soup = BeautifulSoup(resp.text, 'html.parser')
        for article in soup.select('.article-content'):
            articles.append(article.get_text(strip=True))
    return articles
```

### 3. 合成数据生成
```python
# 用大模型生成训练数据
from openai import OpenAI

client = OpenAI()

def generate_training_sample(topic: str) -> dict:
    """让 GPT-4 生成一个问答对"""
    response = client.chat.completions.create(
        model="gpt-4",
        messages=[{
            "role": "user",
            "content": f"生成一个关于{topic}的问答对，JSON格式：{{\"question\":\"...\", \"answer\":\"...\"}}"
        }]
    )
    return eval(response.choices[0].message.content)
```

**合成数据方法论：**
- **Self-Instruct**：用种子任务 + 模型自我生成新任务
- **Evol-Instruct**：逐步增加指令复杂度
- **Backtranslation**：反向翻译增加多样性

## 二、数据清洗流水线

### 清洗项目清单

```python
import re
from typing import List, Dict

class DataCleaner:
    """通用数据清洗器"""
    
    @staticmethod
    def remove_duplicates(data: List[Dict], key: str = "text") -> List[Dict]:
        """基于文本相似度去重"""
        seen = set()
        unique = []
        for item in data:
            # 使用 minhash 或 simhash 做近似去重更佳
            text_hash = hash(item[key][:200])  # 简化示例
            if text_hash not in seen:
                seen.add(text_hash)
                unique.append(item)
        return unique
    
    @staticmethod
    def filter_by_length(text: str, min_len=10, max_len=4096) -> bool:
        """过滤过长或过短的文本"""
        return min_len <= len(text) <= max_len
    
    @staticmethod
    def remove_pii(text: str) -> str:
        """隐私信息脱敏（简化版）"""
        # 手机号
        text = re.sub(r'1[3-9]\d{9}', '[PHONE]', text)
        # 身份证
        text = re.sub(r'\d{17}[\dXx]', '[ID_CARD]', text)
        # 邮箱
        text = re.sub(r'\S+@\S+\.\S+', '[EMAIL]', text)
        return text
    
    @staticmethod
    def normalize_whitespace(text: str) -> str:
        """空白字符规范化"""
        text = re.sub(r'\s+', ' ', text)
        return text.strip()
    
    @staticmethod
    def filter_low_quality(text: str) -> bool:
        """过滤低质量文本"""
        # 乱码检测
        if sum(1 for c in text if '一' <= c <= '鿿') / max(len(text), 1) < 0.1:
            return False
        # 重复字符过多
        if max(text.count(c) for c in set(text)) / len(text) > 0.5:
            return False
        return True
```

### 完整清洗流水线
```python
def clean_pipeline(raw_data: List[Dict]) -> List[Dict]:
    cleaner = DataCleaner()
    cleaned = []
    
    for item in raw_data:
        text = item.get("text", "")
        
        # Step 1: 长度过滤
        if not cleaner.filter_by_length(text):
            continue
        
        # Step 2: 空白规范化
        text = cleaner.normalize_whitespace(text)
        
        # Step 3: PII 脱敏
        text = cleaner.remove_pii(text)
        
        # Step 4: 质量过滤
        if not cleaner.filter_low_quality(text):
            continue
        
        item["text"] = text
        cleaned.append(item)
    
    # Step 5: 去重
    cleaned = cleaner.remove_duplicates(cleaned)
    
    return cleaned
```

## 三、数据格式规范化

### 常见训练数据格式

**Alpaca 格式（指令微调）：**
```json
[
  {
    "instruction": "将下面的话翻译成英文",
    "input": "今天的会议推迟到下午三点",
    "output": "Today's meeting has been postponed to 3 PM."
  }
]
```

**ShareGPT 格式（对话微调）：**
```json
[
  {
    "conversations": [
      {"from": "human", "value": "你好"},
      {"from": "gpt", "value": "你好！有什么可以帮你的？"},
      {"from": "human", "value": "介绍一下Python"},
      {"from": "gpt", "value": "Python是一种..."}
    ]
  }
]
```

## 四、数据质量评估

```python
def data_quality_report(dataset: List[Dict]) -> Dict:
    """生成数据质量报告"""
    total = len(dataset)
    if total == 0:
        return {"error": "数据集为空"}
    
    lengths = [len(item.get("text", "")) for item in dataset]
    
    return {
        "样本总数": total,
        "平均长度": sum(lengths) / total,
        "最短样本": min(lengths),
        "最长样本": max(lengths),
        "空样本数": sum(1 for l in lengths if l == 0),
        "长度分布": {
            "<100字": sum(1 for l in lengths if l < 100),
            "100-500字": sum(1 for l in lengths if 100 <= l < 500),
            "500-2000字": sum(1 for l in lengths if 500 <= l < 2000),
            ">2000字": sum(1 for l in lengths if l >= 2000),
        }
    }
```

## 总结

数据清洗 = 采集 → 去重 → 过滤 → 脱敏 → 格式统一 → 质量检查

**记住：在脏数据上训练，得到的只能是脏模型。数据的每一分投入，都会在模型效果上得到回报。**
