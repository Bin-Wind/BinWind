# 离线能力评测：断网时应用究竟还能完成什么

“支持离线”可能只表示能打开缓存页面，也可能包含创建、编辑、搜索和稍后同步。评测时需要用真实任务矩阵，而不是只看宣传标签。


先在联网状态准备已有文档、附件和共享内容，然后关闭网络并冷启动应用。分别测试读取、搜索、新建、编辑、删除、附件访问与导出；恢复网络后检查同步顺序、冲突提示和重复数据。再测试长时间离线、存储空间不足与账号令牌过期。记录哪些内容必须预下载，以及用户是否能看出当前同步状态。对关键工作流，离线副本仍要有设备加密、远程退出和备份策略。
