# 应用权限体检：把“始终允许”缩回实际需要

应用更新和长期使用会累积相机、麦克风、定位、通讯录与后台运行权限。每月或每季度按系统权限分类检查，比逐个打开应用更容易发现过度授权。

先看最近使用时间与系统提供的访问记录，再按功能判断：扫码只需使用时相机，天气通常不需精确定位，偶尔上传照片可优先使用系统照片选择器。关闭权限后立即测试核心流程，确认应用能给出清晰降级，而不是反复弹窗。

工作配置、家长控制和辅助功能权限可能由组织或家庭策略管理，不要擅自移除。对不再使用的应用先导出必要数据，再卸载并检查云端授权是否仍存在。权限体检的目标不是全部关闭，而是让访问范围、时间和功能相匹配。
