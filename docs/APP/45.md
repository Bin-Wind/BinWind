# 数据导出验收：确认离开应用后仍能使用

看到“支持导出”不代表真正可迁移。评测应用时应实际导出一份包含文本、附件、标签、时间和关联关系的样本，再在独立工具中打开。

检查格式是否有公开说明、字符编码是否正确、时区和创建时间是否保留、附件是否为原始文件，以及内部链接是否还能映射。导出包应包含清单和失败报告，不能因一个附件错误就静默缺失。大数据量还需测试分卷、断点和过期下载链接。

删除账号前至少保存两份导出并抽查摘要或文件数量；敏感导出要加密存储并及时清理临时副本。同步不是导出，平台内备份也不是可移植副本。真正的数据可携带性，要以另一套软件能够解释和使用为准。
