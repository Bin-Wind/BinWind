# 快捷键设计：既提高效率也保持可发现

快捷键服务高频用户，但功能不能只靠记忆才能找到。菜单、按钮提示和命令面板应展示当前平台对应组合，并允许用户从可见入口完成同一动作。


优先沿用操作系统和同类产品约定，避开浏览器、输入法与辅助技术常用组合。单键快捷键在文本输入和语音输入场景容易误触，应提供关闭或重映射。冲突时明确提示，不要静默覆盖。快捷键表按任务分组，支持搜索，并在用户连续使用某动作后再温和提示。测试不同键盘布局、macOS 与 Windows 修饰键、焦点位于输入框和弹层时的行为。
