# 响应式数据表：小屏幕上保留比较关系

桌面表格依赖行列对齐，直接在手机上压缩会让文字不可读。先判断用户在小屏最重要的是横向比较、查看单条详情，还是完成一个操作。


需要比较时保留表格结构，冻结关键列并允许明确的横向滚动；需要逐条浏览时可转为卡片，但每个值都要带字段标签。次要列允许用户选择显示，不能只按设计师偏好永久隐藏。排序和筛选状态在布局切换后保持一致。触控目标足够大，表头与单元格建立语义关联。用真实长文本、空值、多语言和 200% 放大测试，避免把信息密度问题伪装成断点问题。
