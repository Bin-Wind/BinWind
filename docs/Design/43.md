# 错误提示文案：说明发生了什么以及如何继续

“操作失败”只报告情绪，不提供恢复路径。有效错误提示包含受影响动作、可理解的原因、用户现在能做什么，以及必要时用于支持排查的短错误编号。


把提示放在问题附近：字段错误紧邻字段，页面级故障放在内容区域，系统性中断再使用全局提示。避免责备用户，也不要把内部异常、堆栈或敏感字段暴露出来。可重试错误提供重试并保留输入；权限错误说明需要哪种权限和联系谁；不可逆失败明确当前数据是否已保存。用读屏顺序、键盘焦点和弱网重试测试，指标关注恢复成功率而不只是弹窗关闭率。
