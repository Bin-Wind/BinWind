# 可撤销操作：给高频误触一条低成本回路

删除、归档或移动等高频操作若每次都弹确认框，会打断节奏并让用户习惯性点击。对可恢复动作，可先执行并短时提供“撤销”，把注意力留给真正不可逆的决定。

提示应说明刚发生了什么、影响哪个对象以及撤销期限。撤销按钮保持键盘可达，读屏能及时获知，但不要用倒计时动画制造压力。批量操作要展示数量，并在撤销时恢复完整选择和顺序。

后端可采用延迟提交、软删除或补偿操作，但必须定义并发变化：对象已被其他人编辑、权限撤销或恢复目标位置不存在时，不能假装成功。超过期限后仍应在回收站提供合理恢复窗口。真正不可逆、涉及费用或外部通知的动作仍需事前确认，并明确后果。
