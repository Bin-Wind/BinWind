# 空状态设计：解释为何为空并给出下一步

“暂无数据”没有区分首次使用、筛选无结果、权限不足和加载失败。空状态应先准确反映原因，再提供与当前原因一致的动作。

首次使用可用一句话说明这里将出现什么，并提供创建示例或导入入口；筛选无结果应展示当前条件，允许清除筛选；权限不足说明需要何种访问方式；错误状态则保留重试和诊断信息。不要用插画挤占主要动作，也不要在真实错误时装成友好空白。

空状态仍需保留页面标题、导航和筛选上下文，让用户知道自己在哪里。按钮文案用具体动词，如“创建第一个项目”而不是“开始”。用新账号、已清空账号、无权限账号和断网场景分别测试，避免所有路径落到同一占位组件。
